/** @type {import('next').NextConfig} */
const nextConfig = {
  // Per R3F guidance for Next.js 13.1+ / latest:
  // transpilePackages: ['three']
  transpilePackages: ['three'],
  reactStrictMode: true
};

module.exports = nextConfig;